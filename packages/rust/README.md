# Rust Shared Packages

Shared Rust libraries live in this crate.

- `platform`: cross-service primitives and platform seams.
- `serverkit`: service bootstrap helpers and health contracts.
- `testkit`: deterministic test helpers.

```sh
cargo test --workspace
```
