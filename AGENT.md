# Agent Entry Point

Read `AGENTS.md` first. It is the canonical contract for all coding agents.

Then read the nearest README for the app, service, worker, tool, package, or infra area you are changing. Keep the repository product/domain-first, keep shared language lanes under `packages/<language>`, and use `docs/review-gates.md` as the PR quality bar.
