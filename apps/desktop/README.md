# Desktop Apps

This folder is a framework-neutral desktop surface. It is included for projects that need a desktop client.

Supported setup options live under `options/`:

- `electron`
- `tauri`
- `native`

When a desktop app is not needed, remove this folder during project instantiation.
