# Kubernetes Base

Put reusable Kubernetes manifests here when a project is ready for cluster deployment.

Keep environment-specific overlays out of this folder. Use separate overlay folders such as `infra/kubernetes/dev`, `infra/kubernetes/staging`, and `infra/kubernetes/prod`.
