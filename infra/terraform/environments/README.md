# Terraform Environments

Placeholder for Terraform environment roots. Add real `.tf` files here only after the project chooses Terraform and the target platform.

Expected roots after instantiation:

- `dev`
- `staging`
- `prod`
