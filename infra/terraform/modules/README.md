# Terraform Modules

Placeholder for reusable Terraform modules. Keep modules small, typed, and provider-versioned when Terraform is instantiated.

Expected module areas after instantiation:

- `network`
- `compute`
- `data`
- `security`
- `observability`
